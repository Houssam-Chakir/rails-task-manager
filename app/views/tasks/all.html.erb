<h1>Tasks</h1>
<% @all.each do |task| %>
    <input type="checkbox" name="completed" value=<% task.completed %>>
    <li><%= link_to task.title, details_path(task.id) %></li>
<% end %>
<%= link_to "Create New Task", new_path() %>
